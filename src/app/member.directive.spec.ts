import { MemberDirective } from './member.directive';

describe('MemberDirective', () => {
  it('should create an instance', () => {
    const directive = new MemberDirective();
    expect(directive).toBeTruthy();
  });
});
